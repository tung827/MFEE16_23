extends ../_layouts/master

block content
  h1 註冊表單

  form.form#registerFrm(method="POST", action="/auth/register", enctype="multipart/form-data")
    div
      label(for="email") Email
      input(name="email", type="mail", value="abbe00021@gmail.com")
    div
      label(for="password") 密碼
      input(name="password", type="password", value="testtest")
    div
      label(for="confirmPassword") 確認密碼
      input(name="confirmPassword", type="password", value="testtest")
    div
      label(for="name") 姓名
      input(name="name", type="text", value="彤")
    div
      label(for="photo") 照片
      input(name="photo", type="file")
    button(type="submit") 送出